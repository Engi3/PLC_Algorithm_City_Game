# คู่มือผู้เล่น PLC Algorithm Practice

## สารบัญ

1. [บทนำ](#1-บทนำ)
2. [เริ่มต้นใช้งาน](#2-เริ่มต้นใช้งาน)
3. [วิธีเล่น](#3-วิธีเล่น)
4. [รายการบล็อกคำสั่ง (Instruction Blocks Glossary)](#4-รายการบล็อกคำสั่ง-instruction-blocks-glossary)
5. [การสลับภาษา LD / FBD / ST](#5-การสลับภาษา-ld--fbd--st)
6. [ระบบเศรษฐกิจและคะแนน](#6-ระบบเศรษฐกิจและคะแนน)

---

## 1. บทนำ

**PLC Algorithm Practice** คือแพลตฟอร์มเกมสำหรับฝึกเขียนโปรแกรม PLC (Programmable Logic Controller) แบบ Ladder Logic
ออกแบบมาสำหรับนักศึกษาสาขาแมคคาทรอนิกส์ ผู้เล่นจะได้แก้โจทย์สถานการณ์ระบบอัตโนมัติในโรงงานจริง เช่น
การควบคุมมอเตอร์ สายพานลำเลียง ปั๊มน้ำ สัญญาณไฟจราจร และระบบนับสินค้า โดยต่อวงจร Ladder Logic ด้วยตัวแก้ไขแบบกริด
สไตล์ซอฟต์แวร์อุตสาหกรรม (คลิกวางบล็อก + ลากเดินสาย) แล้วให้ตัวจำลอง (Simulator) ตรวจสอบผลลัพธ์ทันที

ระบบมีผู้ใช้งาน 3 ประเภท:

- **นักศึกษา (Student):** สมัครสมาชิกด้วยชื่อ-นามสกุลและรหัสนักศึกษา บันทึกความคืบหน้า สะสมเหรียญและคะแนนได้
- **ผู้เล่นทดลอง (Guest):** เข้าเล่นได้ทันทีด้วยบัญชี `guest00`-`guest99` โดยไม่ต้องสมัคร แต่ความคืบหน้าจะไม่ถูกบันทึกถาวร
- **อาจารย์ (Teacher):** จัดการรายวิชา สร้าง/แก้ไขด่าน และดูแดชบอร์ดวิเคราะห์ผลนักศึกษา

---

## 2. เริ่มต้นใช้งาน

### เข้าสู่ระบบ / สมัครสมาชิก

- **สมัครสมาชิก (นักศึกษา):** กรอกชื่อ, นามสกุล, รหัสนักศึกษา (Student ID), ชื่อผู้ใช้ (Username) และรหัสผ่าน
  (อย่างน้อย 6 ตัวอักษร) ที่หน้า Register
- **เข้าสู่ระบบ:** ใช้ Username และ Password ที่หน้า Login
- **ทดลองเล่นแบบ Guest:** ใช้ชื่อผู้ใช้ `guest00` ถึง `guest99` โดยรหัสผ่านเป็นค่าเดียวกับชื่อผู้ใช้
  (เช่น `guest00` / `guest00`) ไม่ต้องสมัครสมาชิกก่อน

### แดชบอร์ด (Dashboard)

หลังเข้าสู่ระบบ หน้าแดชบอร์ดของนักศึกษาจะแสดงสถานะปัจจุบัน 4 อย่าง:

| การ์ดสถานะ | ความหมาย |
| --- | --- |
| **Coins** | เหรียญที่สะสมไว้ ใช้ซื้อของในร้านค้า |
| **Energy** | พลังงานที่เหลือ ใช้ในการส่งคำตอบ (Submit) แต่ละครั้ง |
| **Hints** | จำนวนสิทธิ์ขอคำใบ้จาก AI ที่เหลืออยู่ |
| **Skip Tokens** | บัตรข้ามด่านที่ถืออยู่ |

จากแดชบอร์ดสามารถกด **Browse levels** เพื่อไปเลือกด่าน หรือ **Visit shop** เพื่อไปซื้อไอเทม

### พลังงาน (Energy)

- พลังงานสูงสุดคือ **100 หน่วย**
- พลังงานจะฟื้นฟูอัตโนมัติ **+1 หน่วยทุก 60 วินาที** แม้จะไม่ได้เปิดแอปทิ้งไว้ก็ตาม (คำนวณจากเวลาที่ผ่านไปจริง)
- การกด **Submit** ตรวจคำตอบแต่ละครั้งจะใช้พลังงาน **1 หน่วย** (เฉพาะบัญชีนักศึกษา/แขก อาจารย์ไม่เสียพลังงานเมื่อทดสอบด่าน)
- ถ้าพลังงานหมด (0 หน่วย) จะไม่สามารถกด Submit ได้ ต้องรอให้ฟื้นฟู หรือซื้อ **Energy Refill** จากร้านค้า

### เหรียญ (Coins)

- ได้รับเหรียญเมื่อ **ผ่านด่าน (Passed)** และคะแนนที่ได้ **สูงกว่าคะแนนที่ดีที่สุดเดิมของด่านนั้น** เท่านั้น
  (แก้ด่านซ้ำด้วยคะแนนเท่าเดิมหรือต่ำกว่าจะไม่ได้เหรียญเพิ่ม)
- จำนวนเหรียญที่ได้คำนวณจากคะแนน: ประมาณ `คะแนน ÷ 10` (ปัดเศษ) และได้อย่างน้อย 2 เหรียญเสมอเมื่อผ่านด่าน

---

## 3. วิธีเล่น

ตัวแก้ไขวงจร (Grid Editor) เป็นแบบตารางกริดสไตล์ซอฟต์แวร์อุตสาหกรรม (คล้าย GX Works3) ใช้ทั้งใน Sandbox, หน้าเล่นด่าน และหน้าสร้างด่านของอาจารย์

### โครงสร้างวงจร Ladder

- โปรแกรมหนึ่งด่านประกอบด้วย **รัง (Rung)** ได้สูงสุด 12 รัง เรียงจากบนลงล่าง
- แต่ละรังเป็นตารางกว้าง **11 ช่อง (คอลัมน์)** — 10 ช่องแรกไว้วางหน้าสัมผัส/บล็อกเงื่อนไขเรียงต่อกัน ส่วนช่องขวาสุดไว้วางเอาต์พุตของรังเท่านั้น
- แต่ละรังเพิ่ม **แถวขนาน (Branch row)** ได้สูงสุด 6 แถว กด **"+ Insert Branch Row (OR)"** เพื่อเพิ่มแถวใหม่
- การเดินสาย (Wiring) ทำได้อย่างอิสระระหว่างช่องใดก็ได้ในรังเดียวกัน ไม่จำกัดแค่เรียงต่อกันเป็นเส้นตรง — ลากสายข้ามแถว ข้ามคอลัมน์ หรือแยกแขนงกลางเส้นได้ (ดูหัวข้อ "การเดินสาย" ด้านล่าง)

**กฎตรรกะสำคัญ:**

- หน้าสัมผัสที่ **ต่ออนุกรมกัน (เรียงในเส้นเดียวกัน)** = เงื่อนไข **AND** (ต้องจริงพร้อมกันทุกตัว)
- แขนงที่ **ต่อขนานกัน (คนละแถวแต่ไปบรรจบจุดเดียวกัน)** = เงื่อนไข **OR** (จริงตัวใดตัวหนึ่งก็พอ)

### การวางบล็อก (คีย์ลัด F5-F10)

คลิกเลือกช่องว่างในกริดที่ต้องการ (ช่องจะไฮไลต์) แล้วกดคีย์ลัดเพื่อวางบล็อกทันที แทนการลากจาก Palette แบบเดิม:

| คีย์ | บล็อก |
| --- | --- |
| **F5** | NO — หน้าสัมผัสปกติเปิด |
| **F6** | NC — หน้าสัมผัสปกติปิด |
| **F7** | COIL — เอาต์พุต (วางได้เฉพาะช่องขวาสุดของรัง) |
| **F8** | คำสั่งประยุกต์ — กด **ซ้ำ** เพื่อวนเลือก: SET → RESET → CMP (เปรียบเทียบค่า) → TIMER → COUNTER |
| **F9** | โหมดวาดเส้นไฟ (Draw Line) — คลิกจุดเชื่อมสองจุดเพื่อต่อสาย |
| **F10** | โหมดลบเส้นไฟ (Delete Line) — คลิกจุดเชื่อมที่ต้องการตัดสาย |
| **Delete / Backspace** | ลบบล็อกที่เลือกอยู่ |

หลังวางบล็อกแล้ว เลือก **แอดเดรส (Address)** จากดรอปดาวน์เหมือนเดิม (`X0`-`X7`, `Y0`-`Y3`, `T0`-`T3`, `C0`-`C3` หรือชื่อตัวแปรที่ประกาศเองผ่านปุ่ม **"+ Add Variable"**) สำหรับ Timer/Counter ต้องเลือก **ชนิด (Variant)** และตั้งค่า **Preset** เช่นเดิม ส่วนบล็อก **CMP (Comparison)** ต้องเลือกตัวดำเนินการ (>, <, =, ≥, ≤) และค่าที่จะเทียบ

### การเดินสาย (Wiring)

ทุกช่องที่มีบล็อกวางอยู่จะมี **จุดเชื่อมต่อ (Port)** เล็กๆ ที่ขอบช่อง (บน/ล่าง/ซ้าย/ขวา) ใช้เชื่อมสายไฟระหว่างบล็อกใดก็ได้ในรัง ไม่จำกัดแค่เรียงในแถวเดียวกัน:

- **ลากเพื่อเชื่อม (Drag-to-connect):** กดค้างที่จุดเชื่อมต่อแล้วลากไปปล่อยที่จุดเชื่อมต่อของอีกบล็อกหนึ่ง ระบบเดินสายมุมฉากให้อัตโนมัติ
- **แตะสองครั้ง (Two-tap):** สำหรับหน้าจอสัมผัส แตะจุดเชื่อมต่อจุดแรก แล้วแตะจุดเชื่อมต่อปลายทางอีกครั้งเพื่อเชื่อมสาย
- **แยกแขนงกลางเส้น (Mid-wire tap):** แตะ/คลิกกึ่งกลางเส้นไฟที่มีอยู่แล้ว เพื่อเริ่มแยกแขนงใหม่จากจุดนั้นโดยไม่ต้องเริ่มจากปลายบล็อก
- **ลบเส้นไฟ:** คลิกที่เส้นไฟที่ต้องการตัดได้โดยตรง (หรือใช้โหมด F10 ด้านบน)
- ระหว่างลากสายข้ามหลายคอลัมน์ กริดจะ **เลื่อนอัตโนมัติ (Auto-scroll)** เมื่อเมาส์/นิ้วเข้าใกล้ขอบจอ

### ทางลัดสร้างวงจรล็อคตัวเอง (Self-Hold)

เลือกหน้าสัมผัสตัวแรกสุดของแถว แล้วกดปุ่ม **"🔒 Self-Hold"** บนแถบเครื่องมือ ระบบจะสร้างแขนงขนาน (OR) ให้อัตโนมัติเพื่อ "ล็อค" สถานะเอาต์พุตให้ค้างไว้ (วงจร Start/Stop แบบคลาสสิก) โดยไม่ต้องเดินสายเอง

### ประเภทสวิตช์อินพุต (Momentary / Latching)

ที่แผง I/O ปุ่มอินพุตแต่ละปุ่มมีป้ายกำกับ **M** หรือ **L** ที่มุม คลิกป้ายเพื่อสลับ:

- **M (Momentary):** สวิตช์กดปล่อย — เป็นจริงเฉพาะตอนกดค้างไว้เท่านั้น ปล่อยแล้วกลับเป็นเท็จทันที (จำลองปุ่มกด/Push button)
- **L (Latching):** สวิตช์ค้างสถานะ — คลิกครั้งแรกติด คลิกอีกครั้งดับ (จำลองสวิตช์โยก/Toggle switch)

### ตัวจำลอง (Simulator)

- **Step:** สั่งให้เครื่องรันหนึ่งรอบสแกน (scan) และให้ไทเมอร์นับเวลาไปหนึ่งติ๊ก
- **Run / Stop:** สั่งให้รันต่อเนื่องอัตโนมัติ (ติ๊กไทเมอร์ไปเรื่อยๆ) หรือหยุด
- **Reset:** ล้างค่าคอยล์ ไทเมอร์ และเคาน์เตอร์ทั้งหมดกลับเป็นค่าเริ่มต้น
- แผง **I/O Panel** ด้านล่างใช้สลับสถานะอินพุต (จำลองการกดปุ่ม/สวิตช์จริง) และแสดงสถานะคอยล์/ไทเมอร์/เคาน์เตอร์แบบเรียลไทม์

### การส่งคำตอบและคำใบ้

- เมื่อพร้อมแล้วกด **Submit** ระบบจะตรวจคำตอบกับชุดทดสอบ (Test cases) ที่ซ่อนอยู่ของด่านนั้น
  ถ้าผ่านทุกกรณีจะได้คะแนนตามจำนวนบล็อกที่ใช้ (ดูหัวข้อ [คะแนน](#คะแนนของด่าน))
- แต่ละด่านมี **คำใบ้ประจำด่าน (Hints)** ให้กดดูทีละข้อ ("ดูคำใบ้ N/M") เป็นคำใบ้ทั่วไปที่ช่วยชี้แนวทาง
  ไม่ได้เฉลยคำตอบตรงๆ
- ปุ่ม **"Ask AI for a hint"** จะส่งสถานะวงจรปัจจุบันให้ AI วิเคราะห์และแนะนำจุดที่ควรแก้ไข โดยใช้สิทธิ์คำใบ้ (Hint credits) 1 ครั้งต่อการขอ 1 ครั้ง
- ถ้าติดด่านจริงๆ และมี **Skip Token** สามารถกด **"Use Skip Token"** เพื่อข้ามด่านนั้นไปเลย โดยจะได้คะแนนขั้นต่ำของด่าน
  (ใช้ได้เฉพาะด่านที่ยังไม่เคยผ่าน และใช้ได้ครั้งเดียวต่อด่าน)
- หลังผ่านด่านแล้ว ปุ่ม **"ขอรีวิวโค้ดจาก AI (Request AI Review)"** จะส่งวงจรที่ผ่านแล้วให้ AI ประเมินเชิงลึก 4 ด้าน
  (คะแนน 0-100 ต่อด้าน): **ความถูกต้อง (Correctness)**, **ความกระชับ (Conciseness)**, **ความปลอดภัย (Safety)**
  และ **แนวทางการแก้ปัญหา (Approach)** — ด้านหลังนี้ประเมินวิธีคิดของผู้เล่น เช่น ใช้เทคนิคที่ชาญฉลาด
  (self-hold, interlock, การแบ่งแขนงที่เหมาะสม) หรือมีรูปแบบที่ควรปรับปรุง พร้อมคำแนะนำเป็นภาษาไทย
  และอาจได้รับเหรียญโบนัสเพิ่มเติมตามคะแนนเฉลี่ย

---

## 4. รายการบล็อกคำสั่ง (Instruction Blocks Glossary)

| บล็อก | ชื่อเต็ม | ทำงานอย่างไร |
| --- | --- | --- |
| **NO** | Normally Open Contact | หน้าสัมผัสปกติเปิด — "นำกระแส" (ให้ผลเป็นจริง) เมื่อแอดเดรสที่อ้างถึงมีค่า **จริง** |
| **NC** | Normally Closed Contact | หน้าสัมผัสปกติปิด — "นำกระแส" เมื่อแอดเดรสที่อ้างถึงมีค่า **เท็จ** (กลับด้านจาก NO) |
| **COIL** | Output Coil | เอาต์พุตจะ **ทำงานตามเงื่อนไขรังแบบทันที** ไม่ค้างสถานะ — เมื่อรังเป็นจริงคอยล์ติด เมื่อรังเป็นเท็จคอยล์ดับทันที |
| **SET (S)** | Set / Latch | สั่งให้แอดเดรสเป้าหมายเป็น **จริงค้างไว้** เมื่อรังเป็นจริง แม้เงื่อนไขจะกลับเป็นเท็จภายหลัง สถานะก็ยังคงจริงจนกว่าจะมี RESET |
| **RESET (R)** | Reset / Unlatch | ล้างค่าแอดเดรสเป้าหมายกลับเป็นเท็จ (สำหรับคอยล์) หรือล้างค่าสะสมกลับเป็น 0 (สำหรับไทเมอร์/เคาน์เตอร์) เมื่อรังเป็นจริง |
| **TON** | Timer On-Delay | เริ่มนับเวลาสะสมขณะรังเป็นจริง เมื่อค่าสะสมถึง Preset บิต `.DN` จะเป็นจริง (หน่วงเวลาก่อน "ติด") ถ้ารังกลับเป็นเท็จเมื่อใด ค่าสะสมจะรีเซ็ตเป็น 0 ทันที |
| **TOF** | Timer Off-Delay | บิต `.DN` เป็นจริงทันทีที่รังเป็นจริง แต่เมื่อรังกลับเป็นเท็จ จะยังค้าง `.DN` เป็นจริงต่อไปอีกตามเวลา Preset ก่อนจะดับ (หน่วงเวลาก่อน "ดับ") |
| **RTO** | Retentive Timer On | สะสมเวลาขณะรังเป็นจริง เหมือน TON แต่ **ไม่รีเซ็ตค่าสะสมเองอัตโนมัติ** เมื่อรังเป็นเท็จ ต้องใช้บล็อก RESET แยกต่างหากเพื่อล้างค่าสะสมเท่านั้น เหมาะกับงานที่ต้องสะสมเวลารวมแม้หยุดเป็นช่วงๆ |
| **CTU** | Count Up | นับค่าเพิ่มขึ้นทีละ 1 ทุกครั้งที่รังเปลี่ยนจากเท็จเป็นจริง (rising edge) เมื่อค่านับถึง Preset บิต `.DN` จะเป็นจริง |
| **CTD** | Count Down | เริ่มต้นค่านับที่เท่ากับ Preset แล้วนับลดลงทีละ 1 ทุกครั้งที่รังเปลี่ยนจากเท็จเป็นจริง บิต `.DN` จะเป็นจริงเมื่อค่านับลดลงถึง 0 |

### ความเร็วการสแกนและหน่วยเวลาของ Preset (สำคัญ)

ตัวจำลอง (Simulator) ทำงานแบบ **1 ครั้งสแกน = 1 "tick"**:

- กด **Run** → เครื่องสแกนอัตโนมัติ **10 ครั้ง/วินาที** (ทุก 100 มิลลิวินาที) ดังนั้น **1 tick = 0.1 วินาที**
- กด **Step** → สแกน (นับ 1 tick) ทันทีที่กดปุ่ม ไม่ผูกกับเวลาจริง เหมาะสำหรับไล่ดูผลลัพธ์ทีละสเต็ปโดยไม่ต้องรีบ

ค่า **Preset** ของ **Timer** จึงหมายถึง **"จำนวน tick"** ไม่ใช่ "วินาที" โดยตรง เมื่อรันด้วยปุ่ม Run (10 ครั้ง/วินาที):

| Preset (tick) | เวลาจริงเมื่อกด Run |
| --- | --- |
| 1 | 0.1 วินาที |
| 5 | 0.5 วินาที |
| **10** | **1 วินาที** |
| 15 | 1.5 วินาที |
| 20 (ค่าสูงสุดที่ตั้งได้) | 2 วินาที |

> **สูตร: วินาที = Preset ÷ 10** (เมื่อรันด้วยปุ่ม Run) ช่องกรอก Preset ในตัวแก้ไขจำกัดค่าไว้ที่ **1-20** เท่านั้น
> จึงหน่วงเวลาได้สูงสุด 2 วินาทีต่อ Timer หนึ่งตัว หากต้องการหน่วงนานกว่านั้น ให้ต่อ Timer หลายตัวเป็นลำดับขั้น (ดูหัวข้อ `.DN` ด้านล่าง)

**Counter (CTU/CTD)** ไม่เกี่ยวกับเวลาเลย — Preset ของ Counter คือ **จำนวนนับเป้าหมาย** (เช่น preset=12 คือ "นับให้ครบ 12 ครั้ง") นับตามจำนวนครั้งที่รังเปลี่ยนจากเท็จเป็นจริง ไม่ใช่ตามเวลาที่ผ่านไป

### บิต/รีจิสเตอร์ต่อท้ายของ Timer และ Counter

ทั้ง Timer และ Counter อ่านค่าย่อยผ่านแอดเดรสต่อท้ายชื่อได้ (เช่น `T0.DN`, `C0.ACC`) ใช้ร่วมกันทั้งสองชนิด:

| ต่อท้าย | ความหมาย | ชนิดค่า |
| --- | --- | --- |
| **.DN** (Done) | จริงเมื่อทำงานครบตามเงื่อนไข (ครบเวลา/ครบจำนวน) — นำไปใช้เป็นเงื่อนไขของ NO/NC ในรังถัดไปได้เหมือนแอดเดรสทั่วไป เพื่อต่อเป็นลำดับขั้น (chaining) | บิต (จริง/เท็จ) |
| **.EN** (Enable) | จริงเมื่อรังที่ป้อนบล็อกนี้กำลังนำกระแสอยู่ ณ ขณะนั้น | บิต (จริง/เท็จ) |
| **.ACC** (Accumulator) | ค่าที่สะสม/นับได้แล้ว ณ ขณะนั้น (จำนวน tick ที่นับแล้ว หรือจำนวนครั้งที่นับแล้ว) — ใช้กับบล็อกเปรียบเทียบ (CMP) ได้ เช่น `T0.ACC >= 5` | ตัวเลข |
| **.PRE** (Preset) | ค่าเป้าหมายที่ตั้งไว้ อ่านย้อนกลับได้เช่นกัน | ตัวเลข |

### รายละเอียดแต่ละบล็อก

**NO / NC (หน้าสัมผัส)** — อ่านค่าจากแอดเดรสที่ผูกไว้ทุกครั้งที่สแกน ไม่ค้างสถานะเอง เปลี่ยนตามอินพุต/คอยล์ทันที ผูกกับแอดเดรสได้ทั้งอินพุต (X0-X7), คอยล์ (Y0-Y3), ตัวแปร M ที่ประกาศเอง, หรือบิต `.DN`/`.EN` ของ Timer/Counter

**COIL (เอาต์พุตทันที)** — ไม่มีความจำ (non-latching) ต้องมีเงื่อนไขรังเป็นจริง **ตลอดเวลา** คอยล์ถึงจะติด พอเงื่อนไขเป็นเท็จคอยล์ดับทันทีในสแกนถัดไป (ห้ามใช้แอดเดรสเดียวกันเขียนด้วย COIL ซ้ำในรังอื่น — ตัวแก้ไขจะแจ้งเตือน "Address taken")

**SET (S) / RESET (R) — ใช้คู่กันเสมอ** — SET ทำให้แอดเดรสเป้าหมาย **ค้างเป็นจริง** แม้เงื่อนไขรังจะกลับเป็นเท็จภายหลัง (ต่างจาก COIL ที่ดับทันที) ส่วน RESET ใช้ล้างค่ากลับ: สำหรับคอยล์/ตัวแปร M คือทำให้เป็นเท็จ, สำหรับ Timer/Counter คือ **ล้างค่า `.ACC` กลับเป็น 0 และเคลียร์บิต `.DN`** (จำเป็นสำหรับ RTO เพราะมันไม่รีเซ็ตเองอัตโนมัติ) ต้องใช้ทั้งคู่เสมอเป็นแพทเทิร์น Start/Stop (SET เมื่อ Start, RESET เมื่อ Stop) ไม่เช่นนั้นจะไม่มีทางดับ

**TON (Timer On-Delay)** — "หน่วงเวลาก่อนติด"
- ระหว่างรังเป็นจริง: `.ACC` เพิ่มทีละ 1 tick ต่อการสแกน 1 ครั้ง จนถึง Preset
- เมื่อ `.ACC` ≥ Preset → `.DN` เป็นจริง
- ทันทีที่รังกลับเป็นเท็จ (ไม่ว่านับไปถึงไหน) → `.ACC` รีเซ็ตกลับเป็น 0 ทันที และ `.DN` ดับ
- ตัวอย่าง: ต้องการหน่วง **1 วินาที** ก่อนปั๊มทำงาน → ตั้ง Preset = **10** แล้วใช้ `T0.DN` เป็นเงื่อนไขต่อเข้า `COIL(Y0)`

**TOF (Timer Off-Delay)** — "หน่วงเวลาก่อนดับ"
- `.DN` เป็นจริง **ทันที** ที่รังเป็นจริง (ไม่หน่วง)
- เมื่อรังกลับเป็นเท็จ `.DN` จะยัง **ค้างเป็นจริงต่ออีก Preset tick** ก่อนจะดับจริง — เช่น พัดลมระบายความร้อนยังทำงานต่ออีกพักหลังปิดสวิตช์เครื่องจักร

**RTO (Retentive Timer On)** — "สะสมเวลาแบบไม่ลืม"
- นับสะสมเหมือน TON ทุกประการ แต่ **`.ACC` ไม่รีเซ็ตเองเมื่อรังเป็นเท็จ** — หยุดนับชั่วคราวแล้วนับต่อจากเดิมได้เมื่อรังกลับมาเป็นจริงอีกครั้ง
- ต้องใช้บล็อก RESET แยกต่างหากเพื่อล้าง `.ACC` กลับเป็น 0 เท่านั้น มิฉะนั้นค่าจะค้างสะสมไปเรื่อยๆ
- เหมาะกับงานที่ต้องรวมเวลาทำงานทั้งหมด (เช่น ชั่วโมงใช้งานเครื่องจักรสะสม) แม้จะหยุดเป็นช่วงๆ

**CTU (Count Up)**
- `.ACC` เพิ่มทีละ 1 **เฉพาะตอนรังเปลี่ยนจากเท็จ→จริง** (ขอบขาขึ้น/rising edge) เท่านั้น ไม่นับซ้ำถ้ารังยังค้างเป็นจริงอยู่
- ไม่เกี่ยวกับเวลา/tick เลย — Preset คือจำนวนครั้งเป้าหมาย
- `.DN` เป็นจริงเมื่อ `.ACC` ≥ Preset และค้างจริงไปจนกว่าจะมี RESET

**CTD (Count Down)**
- `.ACC` เริ่มต้นที่ค่า Preset แล้วลดลงทีละ 1 ทุกขอบขาขึ้นของรัง
- `.DN` เป็นจริงเมื่อ `.ACC` ลดลงถึง 0

### ตัวอย่างการใช้งานจริง

- **มอเตอร์ทำงานเมื่อกด Start และ Enable พร้อมกัน:** วาง `NO(X0)` ต่ออนุกรมกับ `NO(X1)` แล้วต่อเข้า `COIL(Y0)` (AND)
- **ประตูเปิดเมื่อกดปุ่มใดปุ่มหนึ่งจาก 2 จุด:** วาง `NO(X0)` และ `NO(X1)` คนละบรานช์ (ขนาน) แล้วต่อเข้า `COIL(Y0)` (OR)
- **สายพานติดค้างจนกว่าจะกด Stop (Start/Stop latch):**
  รังที่ 1: `NO(X0)` → `SET(Y0)`  
  รังที่ 2: `NO(X1)` → `RESET(Y0)`
- **ปั๊มน้ำเริ่มทำงานหลังกดค้าง 1 วินาที:** `NO(X0)` → `TON(T0, preset=10)` แล้วอีกรังใช้ `NO(T0.DN)` → `COIL(Y0)`
- **นับกล่องครบ 12 ขวดแล้วส่งสัญญาณ:** `NO(X0)` → `CTU(C0, preset=12)` แล้วอีกรังใช้ `NO(C0.DN)` → `COIL(Y1)`

---

## 5. การสลับภาษา LD / FBD / ST

หน้าเล่นด่านมีแท็บสลับมุมมอง 3 แบบ ซึ่งแสดงตรรกะเดียวกันในรูปแบบภาษามาตรฐาน PLC ที่ต่างกัน (ตามมาตรฐาน IEC 61131-3):

- **Ladder Diagram (LD):** มุมมองหลักของ Grid Editor ที่ใช้วางบล็อกและเดินสาย แก้ไขได้โดยตรง
- **Function Block Diagram (FBD):** แสดงตรรกะเดียวกันในรูปแบบบล็อกฟังก์ชันเชื่อมต่อกัน เหมาะสำหรับดูภาพรวมการไหลของสัญญาณ (มุมมองสำหรับดูอย่างเดียว)
- **Structured Text (ST):** แสดงโค้ดข้อความที่เทียบเท่ากับวงจร Ladder ที่สร้างไว้ เหมาะสำหรับฝึกอ่านโค้ด PLC แบบข้อความ (มุมมองสำหรับดูอย่างเดียว)

การสลับไปมาระหว่าง 3 แท็บนี้ช่วยให้เข้าใจว่าตรรกะเดียวกันสามารถเขียนได้หลายรูปแบบ ซึ่งเป็นทักษะสำคัญเมื่อทำงานกับ PLC ยี่ห้อ/รุ่นต่างๆ ในโรงงานจริง

---

## 6. ระบบเศรษฐกิจและคะแนน

### คะแนนของด่าน

- แต่ละด่านมี **จำนวนบล็อกที่เหมาะสมที่สุด (Optimal blocks count)** กำหนดไว้
- คะแนนเต็มคือ **100 คะแนน** เมื่อผ่านด่านด้วยจำนวนบล็อกเท่ากับหรือน้อยกว่าค่าที่เหมาะสมที่สุด
- ถ้าใช้บล็อกเกินกว่าค่าที่เหมาะสมที่สุด จะถูกหักคะแนน **10 คะแนนต่อ 1 บล็อกที่เกิน**
- คะแนนต่ำสุดของการผ่านด่าน (รวมถึงกรณีใช้ Skip Token) คือ **20 คะแนน** เสมอ — ไม่มีทางได้ 0 คะแนนถ้าผ่านด่านแล้ว
- ระบบจะบันทึก **คะแนนที่ดีที่สุด (Best score)** ของแต่ละด่านไว้ ลองแก้ใหม่ให้ได้คะแนนสูงขึ้นได้เสมอ

### ใบประกาศนียบัตร (Certificates)

หน้า **My Progress** จะปลดล็อกใบประกาศนียบัตรแยกตามสมรรถนะ 6 ด้าน เมื่อคะแนนด้านนั้นถึง **80/100** ขึ้นไป
สำหรับ 2 ด้านที่คำนวณจากการเล่นด่านโดยตรง คือ **การเขียนโปรแกรมแลดเดอร์ (Ladder Programming)** และ
**การแก้ปัญหา (Problem Solving)** มีเงื่อนไขเพิ่มอีกข้อ: ต้องมี **คะแนนเฉลี่ยจากด่านทั้งหมดในระบบ (ไม่ใช่แค่ด่านที่เคยเล่น) ถึง 80%
ขึ้นไปด้วย** — เพื่อให้ใบประกาศนียบัตร 2 ด้านนี้สะท้อนความสามารถโดยรวมจริงๆ ไม่ใช่แค่ทำเพียงไม่กี่ด่านให้ได้คะแนนสูง
ส่วนอีก 4 ด้าน (ทักษะการเดินสาย, การแก้ไขข้อบกพร่อง, ความท้าทายขั้นสูง, การควบคุมระบบ) ไม่มีเงื่อนไขนี้ - ใช้เกณฑ์คะแนน
≥80 ตามปกติ ใบประกาศนียบัตรที่ปลดล็อกแล้วดาวน์โหลดเป็น PDF พร้อม QR code สำหรับตรวจสอบความถูกต้องออนไลน์ได้

### ร้านค้า (Shop)

ใช้เหรียญ (Coins) ที่สะสมได้แลกไอเทมช่วยเล่น:

| ไอเทม | ราคา | ผลลัพธ์ |
| --- | --- | --- |
| **เติมพลังงาน (Energy Refill)** | 15 เหรียญ | เพิ่มพลังงานทันที 20 หน่วย (สูงสุดไม่เกิน 100) |
| **ปลดล็อกคำใบ้ (Hint Unlocker)** | 10 เหรียญ | เพิ่มสิทธิ์ขอคำใบ้จาก AI อีก 3 ครั้ง |
| **บัตรข้ามด่าน (Skip Level Token)** | 30 เหรียญ | ใช้ข้ามด่านที่ติดอยู่ได้ 1 ด่าน (ได้คะแนนขั้นต่ำ 20 คะแนน) |

### สรุปความสัมพันธ์ของระบบ

1. เล่นด่าน → ใช้พลังงาน 1 หน่วยต่อการกด Submit 1 ครั้ง
2. ผ่านด่านด้วยคะแนนที่ดีขึ้น → ได้เหรียญตามคะแนน (≥ 2 เหรียญ)
3. นำเหรียญไปซื้อพลังงาน/คำใบ้/บัตรข้ามด่านในร้านค้า เพื่อเล่นต่อได้มากขึ้นและผ่านด่านที่ยากขึ้น

ขอให้สนุกกับการฝึกเขียนโปรแกรม PLC ใน PLC Algorithm Practice!
