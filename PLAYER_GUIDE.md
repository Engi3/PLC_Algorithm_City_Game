# คู่มือผู้เล่น PLC Algorithm City

## สารบัญ

1. [บทนำ](#1-บทนำ)
2. [เริ่มต้นใช้งาน](#2-เริ่มต้นใช้งาน)
3. [วิธีเล่น](#3-วิธีเล่น)
4. [รายการบล็อกคำสั่ง (Instruction Blocks Glossary)](#4-รายการบล็อกคำสั่ง-instruction-blocks-glossary)
5. [การสลับภาษา LD / FBD / ST](#5-การสลับภาษา-ld--fbd--st)
6. [ระบบเศรษฐกิจและคะแนน](#6-ระบบเศรษฐกิจและคะแนน)

---

## 1. บทนำ

**PLC Algorithm City** คือแพลตฟอร์มเกมสำหรับฝึกเขียนโปรแกรม PLC (Programmable Logic Controller) แบบ Ladder Logic
ออกแบบมาสำหรับนักศึกษาสาขาแมคคาทรอนิกส์ ผู้เล่นจะได้แก้โจทย์สถานการณ์ระบบอัตโนมัติในโรงงานจริง เช่น
การควบคุมมอเตอร์ สายพานลำเลียง ปั๊มน้ำ สัญญาณไฟจราจร และระบบนับสินค้า โดยต่อวงจร Ladder Logic ด้วยการลากวาง
(Drag-and-Drop) แล้วให้ตัวจำลอง (Simulator) ตรวจสอบผลลัพธ์ทันที

ระบบมีผู้ใช้งาน 3 ประเภท:

- **นักศึกษา (Student):** สมัครสมาชิกด้วยชื่อ-นามสกุลและรหัสนักศึกษา บันทึกความคืบหน้า สะสมเหรียญและคะแนนได้
- **ผู้เล่นทดลอง (Guest):** เข้าเล่นได้ทันทีด้วยบัญชี `guest00`-`guest99` โดยไม่ต้องสมัคร แต่ความคืบหน้าจะไม่ถูกบันทึกถาวร
- **อาจารย์ (Teacher):** จัดการรายวิชา สร้าง/แก้ไขด่าน และดูแดชบอร์ดวิเคราะห์ผลนักศึกษา

---

## 2. เริ่มต้นใช้งาน

### เข้าสู่ระบบ / สมัครสมาชิก

- **สมัครสมาชิก (นักศึกษา):** กรอกชื่อ, นามสกุล, รหัสนักศึกษา (Student ID), ชื่อผู้ใช้ (Username) และรหัสผ่าน
  (อย่างน้อย 6 ตัวอักษร) ที่หน้า Register
- **เข้าสู่ระบบ:** ใช้ Username และ Password ที่หน้า Login
- **ทดลองเล่นแบบ Guest:** ใช้ชื่อผู้ใช้ `guest00` ถึง `guest99` โดยรหัสผ่านเป็นค่าเดียวกับชื่อผู้ใช้
  (เช่น `guest00` / `guest00`) ไม่ต้องสมัครสมาชิกก่อน

### แดชบอร์ด (Dashboard)

หลังเข้าสู่ระบบ หน้าแดชบอร์ดของนักศึกษาจะแสดงสถานะปัจจุบัน 4 อย่าง:

| การ์ดสถานะ | ความหมาย |
| --- | --- |
| **Coins** | เหรียญที่สะสมไว้ ใช้ซื้อของในร้านค้า |
| **Energy** | พลังงานที่เหลือ ใช้ในการส่งคำตอบ (Submit) แต่ละครั้ง |
| **Hints** | จำนวนสิทธิ์ขอคำใบ้จาก AI ที่เหลืออยู่ |
| **Skip Tokens** | บัตรข้ามด่านที่ถืออยู่ |

จากแดชบอร์ดสามารถกด **Browse levels** เพื่อไปเลือกด่าน หรือ **Visit shop** เพื่อไปซื้อไอเทม

### พลังงาน (Energy)

- พลังงานสูงสุดคือ **100 หน่วย**
- พลังงานจะฟื้นฟูอัตโนมัติ **+1 หน่วยทุก 60 วินาที** แม้จะไม่ได้เปิดแอปทิ้งไว้ก็ตาม (คำนวณจากเวลาที่ผ่านไปจริง)
- การกด **Submit** ตรวจคำตอบแต่ละครั้งจะใช้พลังงาน **1 หน่วย** (เฉพาะบัญชีนักศึกษา/แขก อาจารย์ไม่เสียพลังงานเมื่อทดสอบด่าน)
- ถ้าพลังงานหมด (0 หน่วย) จะไม่สามารถกด Submit ได้ ต้องรอให้ฟื้นฟู หรือซื้อ **Energy Refill** จากร้านค้า

### เหรียญ (Coins)

- ได้รับเหรียญเมื่อ **ผ่านด่าน (Passed)** และคะแนนที่ได้ **สูงกว่าคะแนนที่ดีที่สุดเดิมของด่านนั้น** เท่านั้น
  (แก้ด่านซ้ำด้วยคะแนนเท่าเดิมหรือต่ำกว่าจะไม่ได้เหรียญเพิ่ม)
- จำนวนเหรียญที่ได้คำนวณจากคะแนน: ประมาณ `คะแนน ÷ 10` (ปัดเศษ) และได้อย่างน้อย 2 เหรียญเสมอเมื่อผ่านด่าน

---

## 3. วิธีเล่น

### โครงสร้างวงจร Ladder

- โปรแกรมหนึ่งด่านประกอบด้วย **รัง (Rung)** ได้สูงสุด 12 รัง เรียงจากบนลงล่าง
- แต่ละรังมี **บรานช์ (Branch)** ได้สูงสุด 3 บรานช์ (วางขนานกัน) กด **"+ Add parallel branch"** เพื่อเพิ่มบรานช์ใหม่
- แต่ละบรานช์มีช่องวางหน้าสัมผัส (Contact) เรียงต่อกันได้สูงสุด 4 ช่อง
- ปลายรังแต่ละรังมี **ช่องเอาต์พุต (Output slot)** เพียง 1 ช่อง

**กฎตรรกะสำคัญ:**

- หน้าสัมผัสที่เรียง **ต่อกันในบรานช์เดียวกัน (อนุกรม)** = เงื่อนไข **AND** (ต้องจริงพร้อมกันทุกตัว)
- บรานช์ที่วาง **ขนานกันคนละแถว** = เงื่อนไข **OR** (จริงตัวใดตัวหนึ่งก็พอ)

### การลากวางบล็อก (Drag-and-Drop)

1. ลากบล็อกจาก **แผงบล็อกคำสั่ง (Palette)** ด้านบนไปวางบนช่องว่างในรัง
2. บล็อกหน้าสัมผัส (NO/NC) วางในช่องกลางบรานช์ ส่วนบล็อกเอาต์พุต (COIL/SET/RESET/TIMER/COUNTER) วางที่ช่องเอาต์พุตท้ายรัง
3. หลังวางบล็อกแล้ว เลือก **แอดเดรส (Address)** จากดรอปดาวน์ เช่น อินพุต `I0`-`I7`, คอยล์ `Q0`-`Q3`,
   ไทเมอร์ `T0`-`T3`, เคาน์เตอร์ `C0`-`C3`
4. สำหรับ Timer/Counter ต้องเลือก **ชนิด (Variant)** เช่น TON/TOF/RTO หรือ CTU/CTD และตั้งค่า **Preset**
5. ลบบล็อกที่วางผิดได้ด้วยปุ่มลบที่แสดงเมื่อชี้ที่บล็อกนั้น

### ตัวจำลอง (Simulator)

- **Step:** สั่งให้เครื่องรันหนึ่งรอบสแกน (scan) และให้ไทเมอร์นับเวลาไปหนึ่งติ๊ก
- **Run / Stop:** สั่งให้รันต่อเนื่องอัตโนมัติ (ติ๊กไทเมอร์ไปเรื่อยๆ) หรือหยุด
- **Reset:** ล้างค่าคอยล์ ไทเมอร์ และเคาน์เตอร์ทั้งหมดกลับเป็นค่าเริ่มต้น
- แผง **I/O Panel** ด้านล่างใช้สลับสถานะอินพุต (จำลองการกดปุ่ม/สวิตช์จริง) และแสดงสถานะคอยล์/ไทเมอร์/เคาน์เตอร์แบบเรียลไทม์

### การส่งคำตอบและคำใบ้

- เมื่อพร้อมแล้วกด **Submit** ระบบจะตรวจคำตอบกับชุดทดสอบ (Test cases) ที่ซ่อนอยู่ของด่านนั้น
  ถ้าผ่านทุกกรณีจะได้คะแนนตามจำนวนบล็อกที่ใช้ (ดูหัวข้อ [คะแนน](#คะแนนของด่าน))
- แต่ละด่านมี **คำใบ้ประจำด่าน (Hints)** ให้กดดูทีละข้อ ("ดูคำใบ้ N/M") เป็นคำใบ้ทั่วไปที่ช่วยชี้แนวทาง
  ไม่ได้เฉลยคำตอบตรงๆ
- ปุ่ม **"Ask AI for a hint"** จะส่งสถานะวงจรปัจจุบันให้ AI วิเคราะห์และแนะนำจุดที่ควรแก้ไข โดยใช้สิทธิ์คำใบ้ (Hint credits) 1 ครั้งต่อการขอ 1 ครั้ง
- ถ้าติดด่านจริงๆ และมี **Skip Token** สามารถกด **"Use Skip Token"** เพื่อข้ามด่านนั้นไปเลย โดยจะได้คะแนนขั้นต่ำของด่าน
  (ใช้ได้เฉพาะด่านที่ยังไม่เคยผ่าน และใช้ได้ครั้งเดียวต่อด่าน)

---

## 4. รายการบล็อกคำสั่ง (Instruction Blocks Glossary)

| บล็อก | ชื่อเต็ม | ทำงานอย่างไร |
| --- | --- | --- |
| **NO** | Normally Open Contact | หน้าสัมผัสปกติเปิด — "นำกระแส" (ให้ผลเป็นจริง) เมื่อแอดเดรสที่อ้างถึงมีค่า **จริง** |
| **NC** | Normally Closed Contact | หน้าสัมผัสปกติปิด — "นำกระแส" เมื่อแอดเดรสที่อ้างถึงมีค่า **เท็จ** (กลับด้านจาก NO) |
| **COIL** | Output Coil | เอาต์พุตจะ **ทำงานตามเงื่อนไขรังแบบทันที** ไม่ค้างสถานะ — เมื่อรังเป็นจริงคอยล์ติด เมื่อรังเป็นเท็จคอยล์ดับทันที |
| **SET (S)** | Set / Latch | สั่งให้แอดเดรสเป้าหมายเป็น **จริงค้างไว้** เมื่อรังเป็นจริง แม้เงื่อนไขจะกลับเป็นเท็จภายหลัง สถานะก็ยังคงจริงจนกว่าจะมี RESET |
| **RESET (R)** | Reset / Unlatch | ล้างค่าแอดเดรสเป้าหมายกลับเป็นเท็จ (สำหรับคอยล์) หรือล้างค่าสะสมกลับเป็น 0 (สำหรับไทเมอร์/เคาน์เตอร์) เมื่อรังเป็นจริง |
| **TON** | Timer On-Delay | เริ่มนับเวลาสะสมขณะรังเป็นจริง เมื่อค่าสะสมถึง Preset บิต `.DN` จะเป็นจริง (หน่วงเวลาก่อน "ติด") ถ้ารังกลับเป็นเท็จเมื่อใด ค่าสะสมจะรีเซ็ตเป็น 0 ทันที |
| **TOF** | Timer Off-Delay | บิต `.DN` เป็นจริงทันทีที่รังเป็นจริง แต่เมื่อรังกลับเป็นเท็จ จะยังค้าง `.DN` เป็นจริงต่อไปอีกตามเวลา Preset ก่อนจะดับ (หน่วงเวลาก่อน "ดับ") |
| **RTO** | Retentive Timer On | สะสมเวลาขณะรังเป็นจริง เหมือน TON แต่ **ไม่รีเซ็ตค่าสะสมเองอัตโนมัติ** เมื่อรังเป็นเท็จ ต้องใช้บล็อก RESET แยกต่างหากเพื่อล้างค่าสะสมเท่านั้น เหมาะกับงานที่ต้องสะสมเวลารวมแม้หยุดเป็นช่วงๆ |
| **CTU** | Count Up | นับค่าเพิ่มขึ้นทีละ 1 ทุกครั้งที่รังเปลี่ยนจากเท็จเป็นจริง (rising edge) เมื่อค่านับถึง Preset บิต `.DN` จะเป็นจริง |
| **CTD** | Count Down | เริ่มต้นค่านับที่เท่ากับ Preset แล้วนับลดลงทีละ 1 ทุกครั้งที่รังเปลี่ยนจากเท็จเป็นจริง บิต `.DN` จะเป็นจริงเมื่อค่านับลดลงถึง 0 |

### ตัวอย่างการใช้งานจริง

- **มอเตอร์ทำงานเมื่อกด Start และ Enable พร้อมกัน:** วาง `NO(I0)` ต่ออนุกรมกับ `NO(I1)` แล้วต่อเข้า `COIL(Q0)` (AND)
- **ประตูเปิดเมื่อกดปุ่มใดปุ่มหนึ่งจาก 2 จุด:** วาง `NO(I0)` และ `NO(I1)` คนละบรานช์ (ขนาน) แล้วต่อเข้า `COIL(Q0)` (OR)
- **สายพานติดค้างจนกว่าจะกด Stop (Start/Stop latch):**
  รังที่ 1: `NO(I0)` → `SET(Q0)`  
  รังที่ 2: `NO(I1)` → `RESET(Q0)`
- **ปั๊มน้ำเริ่มทำงานหลังกดค้าง 3 วินาที:** `NO(I0)` → `TON(T0, preset=3)` แล้วอีกรังใช้ `NO(T0.DN)` → `COIL(Q0)`
- **นับกล่องครบ 12 ขวดแล้วส่งสัญญาณ:** `NO(I0)` → `CTU(C0, preset=12)` แล้วอีกรังใช้ `NO(C0.DN)` → `COIL(Q1)`

> หมายเหตุ: `.DN` คือบิต "Done" ของไทเมอร์/เคาน์เตอร์ ใช้เป็นเงื่อนไขหน้าสัมผัสในรังถัดไปได้เหมือนแอดเดรสทั่วไป
> เพื่อต่อเป็นลำดับขั้น (chaining) ได้

---

## 5. การสลับภาษา LD / FBD / ST

หน้าเล่นด่านมีแท็บสลับมุมมอง 3 แบบ ซึ่งแสดงตรรกะเดียวกันในรูปแบบภาษามาตรฐาน PLC ที่ต่างกัน (ตามมาตรฐาน IEC 61131-3):

- **Ladder Diagram (LD):** มุมมองหลักที่ใช้ต่อวงจรด้วยการลากวาง แก้ไขได้โดยตรง
- **Function Block Diagram (FBD):** แสดงตรรกะเดียวกันในรูปแบบบล็อกฟังก์ชันเชื่อมต่อกัน เหมาะสำหรับดูภาพรวมการไหลของสัญญาณ (มุมมองสำหรับดูอย่างเดียว)
- **Structured Text (ST):** แสดงโค้ดข้อความที่เทียบเท่ากับวงจร Ladder ที่สร้างไว้ เหมาะสำหรับฝึกอ่านโค้ด PLC แบบข้อความ (มุมมองสำหรับดูอย่างเดียว)

การสลับไปมาระหว่าง 3 แท็บนี้ช่วยให้เข้าใจว่าตรรกะเดียวกันสามารถเขียนได้หลายรูปแบบ ซึ่งเป็นทักษะสำคัญเมื่อทำงานกับ PLC ยี่ห้อ/รุ่นต่างๆ ในโรงงานจริง

---

## 6. ระบบเศรษฐกิจและคะแนน

### คะแนนของด่าน

- แต่ละด่านมี **จำนวนบล็อกที่เหมาะสมที่สุด (Optimal blocks count)** กำหนดไว้
- คะแนนเต็มคือ **100 คะแนน** เมื่อผ่านด่านด้วยจำนวนบล็อกเท่ากับหรือน้อยกว่าค่าที่เหมาะสมที่สุด
- ถ้าใช้บล็อกเกินกว่าค่าที่เหมาะสมที่สุด จะถูกหักคะแนน **10 คะแนนต่อ 1 บล็อกที่เกิน**
- คะแนนต่ำสุดของการผ่านด่าน (รวมถึงกรณีใช้ Skip Token) คือ **20 คะแนน** เสมอ — ไม่มีทางได้ 0 คะแนนถ้าผ่านด่านแล้ว
- ระบบจะบันทึก **คะแนนที่ดีที่สุด (Best score)** ของแต่ละด่านไว้ ลองแก้ใหม่ให้ได้คะแนนสูงขึ้นได้เสมอ

### ร้านค้า (Shop)

ใช้เหรียญ (Coins) ที่สะสมได้แลกไอเทมช่วยเล่น:

| ไอเทม | ราคา | ผลลัพธ์ |
| --- | --- | --- |
| **เติมพลังงาน (Energy Refill)** | 15 เหรียญ | เพิ่มพลังงานทันที 20 หน่วย (สูงสุดไม่เกิน 100) |
| **ปลดล็อกคำใบ้ (Hint Unlocker)** | 10 เหรียญ | เพิ่มสิทธิ์ขอคำใบ้จาก AI อีก 3 ครั้ง |
| **บัตรข้ามด่าน (Skip Level Token)** | 30 เหรียญ | ใช้ข้ามด่านที่ติดอยู่ได้ 1 ด่าน (ได้คะแนนขั้นต่ำ 20 คะแนน) |

### สรุปความสัมพันธ์ของระบบ

1. เล่นด่าน → ใช้พลังงาน 1 หน่วยต่อการกด Submit 1 ครั้ง
2. ผ่านด่านด้วยคะแนนที่ดีขึ้น → ได้เหรียญตามคะแนน (≥ 2 เหรียญ)
3. นำเหรียญไปซื้อพลังงาน/คำใบ้/บัตรข้ามด่านในร้านค้า เพื่อเล่นต่อได้มากขึ้นและผ่านด่านที่ยากขึ้น

ขอให้สนุกกับการฝึกเขียนโปรแกรม PLC ใน PLC Algorithm City!
