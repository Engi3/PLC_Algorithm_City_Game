/** Phase 14: a student only unlocks a category certificate once that axis's score reaches this threshold. Shared between the client-side unlock check and the server-side public verification page, so they never drift. */
export const CERTIFICATE_THRESHOLD = 80;
